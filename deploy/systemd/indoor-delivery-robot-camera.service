[Unit]
Description=Indoor Delivery Robot MJPEG Camera Stream
Documentation=https://github.com/nattannsra18/amr-base-driver
After=network-online.target
Wants=network-online.target
StartLimitIntervalSec=0

[Service]
Type=simple
User=odroid
Group=odroid
SupplementaryGroups=video
EnvironmentFile=/etc/indoor-delivery-robot/camera.env
ExecStartPre=/usr/bin/test -e ${CAMERA_DEVICE}
ExecStartPre=-/usr/bin/v4l2-ctl --device=${CAMERA_DEVICE} --set-ctrl=exposure_dynamic_framerate=${CAMERA_DYNAMIC_FRAMERATE}
ExecStart=/usr/bin/ustreamer --device=${CAMERA_DEVICE} --format=MJPEG --encoder=HW --resolution=${CAMERA_RESOLUTION} --desired-fps=30 --host=${CAMERA_HOST} --port=${CAMERA_PORT} --buffers=1 --workers=1 --tcp-nodelay --persistent --device-error-delay=2 --no-log-colors
Restart=always
RestartSec=3
TimeoutStopSec=5
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictSUIDSGID=true
MemoryMax=96M
Nice=0

[Install]
WantedBy=multi-user.target
